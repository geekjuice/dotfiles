session_name: claude
windows:
  - window_name: claude
    layout: main-vertical
    options:
      main-pane-width: "50%"
    start_directory: $PWD
    panes:
      - shell_command:
          - cmd: clawed
            enter: false
      - shell_command:
          - cmd: clawed
            enter: false
      - shell_command:
          - cmd: clawed
            enter: false
